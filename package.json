{
  "name": "spartagym-manager",
  "version": "0.1.0",
  "private": true,
  "description": "SpartaGym Manager (repo raíz, código real en /backend y /frontend)",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --omit=dev",
    "start": "node backend/src/app.js",
    "seed": "node backend/src/seed.js"
  }
}
